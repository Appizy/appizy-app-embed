#!/bin/bash

workdir=$( cd "$( dirname "${BASH_SOURCE[0]}" )/../" && pwd )
cmd=$@

if [ ! "" = "$cmd" ]; then
  cmd="yarn $cmd"
fi

docker run \
  --rm \
  -v "$workdir:/var/app" \
  yarn:latest $cmd

